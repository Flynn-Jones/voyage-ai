## How to run the project

If on Windows, use Git Bash or WSL

1. Add executing permission
   ```
   chmod +x up-all.sh
   chmod +x down-all.sh
   ```
2. Starting
   Run `./up-all.sh`
3. Stopping
   Run `./down-all.sh`
